asr:
  sample_rate: 16000     # Sampling rate in Hz
  chunk: 1024            # Buffer size (if needed in future)
  channels: 1            # Mono channel
  duration: 5            # Default recording duration in seconds
  device_index: null     # Use default input device

tts:
  lang: hi               # Text-to-speech language

email:
  to_email: sanikagadade641@gmail.com  # Receiver email
  subject_prefix: "📢 P2P Lending Query"

logging:
  log_file: complaint_log.csv
  log_format: "[{timestamp}] - {query} -> {reply} | {status}"
  fallback_responses:
    - "माफ़ कीजिए, कृपया अपना प्रश्न फिर से कहें।"
    - "मैं समझ नहीं पाया, कृपया और स्पष्ट बोलें।"
    - "कृपया कुछ समय बाद फिर से प्रयास करें।"

locations:
  allowed:
    - mumbai
    - pune
    - delhi
    - kolhapur
    - nashik
